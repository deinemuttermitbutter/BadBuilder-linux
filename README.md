# ABadAvatar-linux
claude version of rewritten Pdawg-bytes BadBuilder for linux


dont complain i dont care about it. its just a one time use case for me. i wont maintain it. it was just a little idea while getting a coffee to just let claude rewrite everything to linux compatible code.
modding xbox360 and i hate windows.

```unzip badbuilder-linux.zip && cd badbuilder-linux
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python3 main.py
```

props to Pdawg-bytes
https://github.com/Pdawg-bytes/BadBuilder
